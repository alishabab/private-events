<h1>All Events</h1>

<h2>Past Events</h2>
  <ul> 
  <% @past.each do |event|%>
  <li>
  <%= link_to event.name, event_path(event) %>
  </li>
  
<%end%>
</ul>

<h2>Future Events</h2>
<ul> 
  <% @future.each do |event|%>
  <li>
  <%= link_to event.name, event_path(event) %>

  </li>
<%end%>
</ul>
